import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import LatexRenderer from './App';

ReactDOM.render(
  <React.StrictMode>
    <LatexRenderer />
  </React.StrictMode>,
  document.getElementById('root')
);

